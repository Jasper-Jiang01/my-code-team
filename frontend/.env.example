# ============================================================
# CodePilot 前端环境变量模板
# 复制本文件为 .env 并填入真实值：cp .env.example .env
# ============================================================

# 后端 origin。同域（Oceanus 站点访问 /api，或本地 Vite 已代理 /api）请留空。
# Talos newtest 页会默认打到 https://codepilot.ai.test.sankuai.com。
# GitHub Pages 没有 /api 反向代理，部署时必须在仓库变量 VITE_API_BASE_URL 填写可访问的后端 origin，
# 并在后端 ALLOWED_ORIGINS 中放行 Pages 域名。
VITE_API_BASE_URL=


# React Bits Pro CLI 授权。请在本机 frontend/.env.local 中填写真实密钥，勿提交到版本库。
REACTBITS_LICENSE_KEY=
