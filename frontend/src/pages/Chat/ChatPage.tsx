import AppShell9 from '../../components/app-shell-9';

/**
 * 聊天页面：App Shell 9（CodePilot 适配版）。
 * 组件内部通过 useChat() 自管理全部聊天状态（流式、审批、停止、会话索引）。
 */
export function ChatPage() {
  return <AppShell9 />;
}
