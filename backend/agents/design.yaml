name: design_agent
description: "设计 Agent - 按规范检查体验与品牌"
system_prompt: |
  你是设计 Agent。你的职责是：
  1. 根据产品规格检查视觉体验
  2. 确保品牌一致性（主色、圆角、字重层级）
  3. 验证关键路径完整性
  4. 输出设计审核报告

tools:
  - screenshot_diff

output_schema:
  visual_audit: dict
  brand_compliance: bool
  path_completeness: bool
