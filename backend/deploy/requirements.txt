# CatPaw / 内网运行依赖（不含 langgraph-api 平台）。
#
# 构建铁律：CloudNative 构建容器为 CentOS 7（glibc 2.17）且默认无 C++ 编译器，
# 且其 gcc 4.8.5 不满足现代 meson 项目的 C++17 要求——任何 native 包回退
# sdist 源码编译都会失败（2026-09-08 contourpy 实例：meson 报
# "Unknown compiler(s): [['c++'], ['g++'], ['clang++']]"）。
# 因此所有含编译产物的传递依赖都显式锁定为内网 PyPI 提供 cp312
# manylinux2014/manylinux_2_17 预编译 wheel 的版本，保证 pip install
# 全程 wheel、零源码编译；纯 Python 包（py3-none-any）无需锁定。
# 新增/升级依赖后必须重跑 wheel 预检补齐锁定（预检失败即说明有包无兼容 wheel）：
#   pip download --dest /tmp/wc --index-url https://pypi.sankuai.com/simple/ \
#     --trusted-host pypi.sankuai.com --only-binary=:all: \
#     --platform manylinux2014_x86_64 --python-version 3.12 \
#     --implementation cp -r requirements.txt

# ===== 直接依赖 =====
langgraph==1.2.11
langchain==1.3.18
langchain-openai==1.6.0
langchain-anthropic==1.7.0
langsmith==0.11.2
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
httpx==0.28.1
psycopg[binary,pool]>=3.2.0
langgraph-checkpoint-sqlite==3.1.1
aiosqlite==0.22.1
langgraph-checkpoint-postgres>=2.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
deepagents>=0.7.13

# ===== 数据分析工具链（analyze_data.py 顶层 import pandas；plot_chart 懒加载 matplotlib）=====
pandas==2.3.2
matplotlib==3.9.4
# tiktoken：内网 PyPI 提供 CPython 3.12 manylinux2014 wheel。
tiktoken==0.7.0
# numpy >= 2.3 只发布 manylinux_2_28 wheel（要求 glibc >= 2.28），会回退 sdist 编译；
# 2.2.6 是内网 PyPI 提供 cp312 manylinux2014 wheel 的最高版本。
numpy==2.2.6

# ===== native 传递依赖锁定（2026-09-08 wheel 预检实测版本；升级前必须重跑预检）=====
# matplotlib 绘图栈
contourpy==1.3.2
kiwisolver==1.5.1
pillow==12.2.0
fonttools==4.64.0
# LLM / JSON 序列化
pydantic-core==2.46.5
jiter==0.16.0
orjson==3.12.0
ormsgpack==1.12.2
regex==2026.9.3
charset-normalizer==3.5.1
# uvicorn[standard]
uvloop==0.22.1
httptools==0.8.0
watchfiles==1.2.0
websockets==16.1.1
# 数据库 / 向量存储
psycopg-binary==3.3.5
sqlite-vec==0.1.9
# 其余 native
cffi==2.1.1
cryptography==50.0.1
pyyaml==6.0.3
uuid-utils==0.17.0
xxhash==4.0.1
zstandard==0.25.0
