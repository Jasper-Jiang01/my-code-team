# CatPaw / 内网运行依赖（不含 langgraph-api 平台）。
langgraph==1.2.11
langchain==1.3.18
langchain-openai==1.6.0
# 内部 PyPI 提供 CPython 3.12 manylinux2014 wheel，避免 CentOS 7 源码编译。
langchain-anthropic==1.7.0
langsmith==0.11.2
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
httpx==0.28.1
psycopg[binary,pool]>=3.2.0
langgraph-checkpoint-sqlite==3.1.1
aiosqlite==0.22.1
langgraph-checkpoint-postgres>=2.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
deepagents>=0.7.13
# 数据分析工具链（analyze_data.py：顶层 import pandas；plot_chart 懒加载 matplotlib）。
# 关键约束：构建容器为 CentOS 7 / glibc 2.17，且没有公网 PyPI 访问，只能使用内网镜像轮子。
# 因此所有 native 依赖都显式锁定到内网 PyPI 提供的 manylinux2014/manylinux_2_17 wheel 最高版本，
# 绝不依赖源码编译，也不再通过 catpaw_deploy.yaml 传 --only-binary（该值会破坏平台生成的外部清单）。
pandas==2.3.2
matplotlib==3.9.4
tiktoken==0.7.0
numpy==2.2.6
