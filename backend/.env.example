# CodePilot 后端环境变量模板
# 复制本文件为 .env 并填入真实值：cp .env.example .env

# ---- LLM ----
# LongCat（美团内部，OpenAI 兼容接口）
# 生产由部署环境变量注入，不要把真实密钥写入仓库
LONGCAT_API_KEY=your_longcat_api_key
LONGCAT_BASE_URL=https://api.longcat.chat/openai
DEFAULT_MODEL=LongCat-2.0

# OpenAI（可选，切换 DEFAULT_MODEL 为 gpt-4o 时需要）
OPENAI_API_KEY=your_openai_api_key

# Anthropic（可选，切换 DEFAULT_MODEL 为 claude-xxx 时需要）
ANTHROPIC_API_KEY=your_anthropic_api_key

# ---- Checkpoint ----
# 可选：auto | memory | sqlite | postgres | platform（默认 auto）
# CHECKPOINT_BACKEND=auto
# CHECKPOINT_SQLITE_PATH=/absolute/path/codepilot.sqlite
# DATABASE_URL=postgresql://user:password@host:5432/codepilot
DATABASE_URL=

# ---- LangSmith 可观测性（可选）----
LANGSMITH_API_KEY=your_langsmith_api_key
LANGSMITH_TRACING=true
LANGSMITH_PROJECT=codepilot

# ---- 内网 FastAPI ----
# 前端域名，逗号分隔；本地和测试域已默认放行
ALLOWED_ORIGINS=
